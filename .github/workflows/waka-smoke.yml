name: WakaTime smoke test (temporary)

# Temporary check for the preview branch, deleted right after it runs. Runs
# the same steps as the update job in waka-stats.yml, but with upstream's
# built-in mock WakaTime data, and only prints the README diff (no commit).
on:
  push:
    branches: [preview/09-wakatime]
    paths: [.github/workflows/waka-smoke.yml]

permissions:
  contents: read

jobs:
  smoke:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v5

      - name: Render WakaTime stats
        id: waka
        uses: anmol098/waka-readme-stats@master
        env:
          DEBUG_RUN: "True"
          INPUT_GH_USER: ${{ github.repository_owner }}
          MOCK_WAKATIME: "True"
          MOCK_DATA_DIR: "."
        with:
          WAKATIME_API_KEY: "mock"
          GH_TOKEN: ${{ github.token }}
          SHOW_TOTAL_CODE_TIME: "True"
          SHOW_LANGUAGE: "True"
          SHOW_EDITORS: "True"
          SHOW_UPDATED_DATE: "True"
          UPDATED_DATE_FORMAT: "%Y-%m-%d"
          SHOW_PROJECTS: "False"
          SHOW_OS: "False"
          SHOW_TIMEZONE: "False"
          SHOW_COMMIT: "False"
          SHOW_DAYS_OF_WEEK: "False"
          SHOW_LANGUAGE_PER_REPO: "False"
          SHOW_LOC_CHART: "False"
          SHOW_LINES_OF_CODE: "False"
          SHOW_PROFILE_VIEWS: "False"
          SHOW_SHORT_INFO: "False"
          SHOW_AI_CODE_TIME: "False"
          SHOW_AI_CODING: "False"

      - name: Write the section into README.md
        env:
          STATS: ${{ steps.waka.outputs.README_CONTENT }}
        run: |
          python3 - <<'EOF'
          import os, re
          stats = os.environ["STATS"].removeprefix("README stats current output:").strip("\n")
          if not stats.strip():
              raise SystemExit("waka-readme-stats rendered nothing")
          start, end = "<!--START_SECTION:waka-->", "<!--END_SECTION:waka-->"
          text = open("README.md", encoding="utf-8").read()
          new, n = re.subn(re.escape(start) + r"[\s\S]*?" + re.escape(end),
                           lambda _: f"{start}\n{stats}\n{end}", text)
          if n != 1:
              raise SystemExit(f"expected one waka section in README.md, found {n}")
          open("README.md", "w", encoding="utf-8").write(new)
          EOF

      - name: Show the README diff (mock data, not committed)
        run: git --no-pager diff -- README.md
